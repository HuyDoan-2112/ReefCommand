# Resources feature

Renders the active resource scenario and lets the demo change it, for example
marking a boat unavailable.

The simulated-data banner is rendered by this feature and is not dismissible.
Simulated capacity is never presented as real.
